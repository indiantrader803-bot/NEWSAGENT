name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *" # Runs every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Dashboard
        run: curl -s https://newsagent-85h8.onrender.com/api/uptime
